{
  "name": "auto-version",
  "version": "1.4.1",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "generate:version": "tsx src/generate-version"
  },
  "pre-push": [
    "generate:version"
  ],
  "devDependencies": {
    "@types/node": "^20.1.3",
    "pre-push": "^0.1.4",
    "tsx": "^3.12.7"
  },
  "dependencies": {
    "typescript": "^5.0.4"
  }
}